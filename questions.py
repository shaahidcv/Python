 #NOTE: # 🌀 **1. Loops in Python**

# ## **For Loop Questions**  
# 1. Print numbers from **1 to 10** using a `for` loop.  ✅
# 2. Calculate the **sum of numbers from 1 to 100**.   ✅
# 3. Find the **factorial of a number** using a `for` loop.   ✅
# 4. Print the **Fibonacci series** up to `n` terms.   ✅
# 5. Iterate over a **dictionary** and print keys and values.   ✅
# 6. Print the **prime numbers** between `1` and `50`.   ✅
# 7. Create a **pattern** using nested loops:    ✅
#     ```
#     *
#     * *
#     * * *
#     * * * *
#     ```  
# 8. Print the **multiplication table** for numbers from `1 to 5`.  ✅

# ## **While Loop Questions**  
# 9. Print numbers from **10 to 1** using a `while` loop.  ✅
# 10. Check if a **number is a palindrome**.   ✅
# 11. Create a `while` loop that keeps asking for input until the user enters `exit`.  ✅
# 12. Find the **sum of digits** of a number using a `while` loop.  ✅
# 13. Create a **number guessing game** using a `while` loop.  ✅

# ## **Avoiding Built-in Functions**  
# 14. Find the **length of a list** without using `len()`.  ✅
# 15. Find the **sum of numbers in a list** without using `sum()`.  ✅
# 16. Find the **maximum number in a list** without using `max()`.   ✅
# 17. Find the **minimum number in a list** without using `min()`.  ✅
# 18. Reverse a list **without using `reverse()` or slicing**.  ✅
# 19. Find the **second largest number in a list** manually.  ✅
# 20. Implement a **custom sort algorithm** (e.g., Bubble Sort).  ❌

# ---

#NOTE:# 📚 **2. List Operations and Comprehensions**

# ## **Basic List Operations**  
# 21. Create a list and find its **sum, max, and min**.  
# 22. Reverse a list using slicing and manually.  
# 23. Remove **duplicates** from a list without using `set()`.  
# 24. Merge two lists and remove duplicates.  
# 25. Create a list of **even and odd numbers** from 1 to 20.

# ## **List Comprehension Questions**  
# 26. Generate a list of **squares from 1 to 10**.  
# 27. Filter out **even numbers** using list comprehension.  
# 28. Flatten a **nested list** using list comprehension.  
# 29. Extract words with **length > 3** from a list.  
# 30. Create a list of tuples containing **number and its square**.

# ## **Avoiding Built-in Functions**  
# 31. Sort a list **without using `sort()` or `sorted()`**.  
# 32. Find the **largest number** in a list manually.  
# 33. Remove duplicates **manually without using `set()`**.  

# ---

#NOTE: # 🔗 **3. Tuple Operations**

# ## **Basic Tuple Operations**  
# 34. Find the **length** of a tuple manually.  
# 35. Convert a **tuple to a list** and vice versa.  
# 36. Find the **index of an element** manually.  
# 37. Find the **largest and smallest** elements in a tuple without using `max()` and `min()`.  
# 38. Unpack a tuple into **individual variables**.

# ## **Avoiding Built-in Functions**  
# 39. Calculate the **sum of tuple elements** manually.  
# 40. Reverse a tuple without slicing.  

# ---

#NOTE: # 🗂️ **4. Set Operations**

# ## **Basic Set Operations**  
# 41. Perform **union, intersection, difference**, and **symmetric difference** on two sets.  
# 42. Check if a **set is a subset or superset** of another.  
# 43. Find **common elements** between two sets.  
# 44. Remove an element from a set using `discard()` and `remove()`.  

# ## **Avoiding Built-in Functions**  
# 45. Implement **union of two sets manually**.  
# 46. Implement **intersection of two sets manually**.  
# 47. Find unique elements from a list **manually without using `set()`**.  

# ---

#NOTE: # 📖 **5. Dictionary Operations**

# ## **Basic Dictionary Operations**  
# 48. Create a dictionary and **print all keys and values**.  
# 49. Add, update, and delete **key-value pairs**.  
# 50. Merge two dictionaries.  
# 51. Create a dictionary of **squares from 1 to 5**.  
# 52. Invert a dictionary (**keys become values and vice versa**).  

# ## **Dictionary Comprehension**  
# 53. Generate a dictionary of **squares of numbers from 1 to 10**.  
# 54. Filter dictionary keys with **values > 10**.  
# 55. Create a dictionary from two lists.  

# ## **Avoiding Built-in Functions**  
# 56. Find the **key with the maximum value** without using `max()`.  
# 57. Check if a **key exists** manually.  
# 58. Merge two dictionaries manually.  

# ---

#NOTE: # ⚡ **6. Lambda and Functional Programming**

# ## **Basic Lambda Usage**  
# 59. Write a lambda function to **find the square of a number**.  
# 60. Create a lambda to **check if a number is even**.  
# 61. Sort a list of tuples by the **second value** using a lambda function.  
# 62. Find the **maximum of two numbers** using lambda.  

# ## **Map, Filter, Reduce with Lambda**  
# 63. Double each number in a list using `map()`.  
# 64. Keep only **odd numbers** using `filter()`.  
# 65. Find the **product of all numbers** using `reduce()`.

# ## **Avoiding Built-in Functions**  
# 66. Implement a custom **`map()`-like function** manually.  
# 67. Implement a custom **`filter()`-like function** manually.  
# 68. Implement a custom **`reduce()`-like function** manually.  

# ---

#NOTE: # 🧠 **7. Algorithms**

# ## **Sorting Algorithms**  
# 69. Implement **Bubble Sort** manually.  ❌
# 70. Implement **Selection Sort** manually.  ❌

# ## **Searching Algorithms**  
# 71. Implement **Linear Search** manually.  ❌
# 72. Implement **Binary Search** manually.  ❌

# ## **Math Challenges**  
# 73. Calculate `x` raised to the power `y` **manually**.  
# 74. Find the **GCD of two numbers manually**.  

# ## **String Challenges**  
# 75. Reverse a string without slicing.  
# 76. Count vowels in a string manually.  
# 77. Find the **most frequent character** in a string.  
# 78. Check if two strings are **anagrams** without using `sorted()`.

# ## **Matrix Operations**  
# 79. Transpose a **2D matrix manually**.  
# 80. Print a **spiral matrix pattern**.

# ---

#NOTE: # 🛠️ **8. Advanced Challenges**

# 81. Create a **number guessing game**.  
# 82. Write a program to **find all prime numbers** up to `n`.  
# 83. Find the **frequency of words** in a string manually.  
# 84. Write a program to **split a string manually**.  
# 85. Create a **custom power function** without using `**` or `pow()`.

# ---

# ### 💡 **Practice Tips:**  
# - Solve challenges **without built-in functions** first.  
# - Use **list comprehensions** creatively.  
# - Experiment with **lambda functions** and **functional programming**.  
# - Write **clean and optimized code**.  
# - Test with **edge cases** (e.g., empty lists, duplicate elements).

# ---

